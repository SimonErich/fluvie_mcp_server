# Fluvie MCP Server Configuration
# Copy this file to .env and customize as needed

# Server Configuration
PORT=8080
HOST=0.0.0.0

# Documentation path (relative or absolute)
# Default: ./data/docs (or /app/data/docs in Docker)
DOCS_PATH=../doc

# Logging level: debug, info, warning, error
LOG_LEVEL=info

# Optional: HTTPS/Traefik Configuration
# Email for Let's Encrypt certificate notifications
ACME_EMAIL=admin@fluvie.at

# Optional: API Key for authentication (leave empty for public access)
# API_KEY=your-secret-api-key

# Optional: Rate limiting (requests per minute)
# RATE_LIMIT=100

# Optional: CORS allowed origins (comma-separated)
# Leave empty to allow all origins
# CORS_ORIGINS=https://fluvie.at,https://docs.fluvie.at
