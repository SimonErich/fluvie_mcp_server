name: fluvie_mcp_server
description: MCP server for Fluvie video composition library - provides documentation search, code generation, and template suggestions
version: 1.0.0
repository: https://github.com/simonerich/fluvie

environment:
  sdk: ^3.0.0

dependencies:
  collection: ^1.18.0
  dotenv: ^4.2.0
  http: ^1.2.0
  json_annotation: ^4.8.1
  logging: ^1.2.0
  markdown: ^7.2.2
  path: ^1.9.0
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  string_similarity: ^2.0.0

dev_dependencies:
  build_runner: ^2.4.7
  json_serializable: ^6.7.1
  lints: ^3.0.0
  test: ^1.25.0
